import { userName, userScore } from './elements.js';

// Reset form to empty
const resetForm = () => {
 userName.value = '';
 userScore.value = '';
};

// Export the function
export default resetForm;